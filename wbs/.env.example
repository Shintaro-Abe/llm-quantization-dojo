# WBS 同期の環境変数テンプレート（ダミー値）。
# このファイルをコピーして wbs/.env を作り、実値を入れる。
# wbs/.env は .gitignore 済み（コミットされない）。

# gws（Google Workspace CLI）が使うサービスアカウント鍵のパス。
# 実鍵は secrets/ に置く（.gitignore 済み）。リポジトリルートからの相対パス、または絶対パス。
GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE=./secrets/your-service-account.json
